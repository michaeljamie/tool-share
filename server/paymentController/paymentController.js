module.exports = {
    updateTool: (req, res) => {
        // const {id} = req.params;
        // const {userid} = req.body;
        // req.app.get('db').([id, userid])
        // .then( response => {
        //     res.status(200)
        // })
    }
}